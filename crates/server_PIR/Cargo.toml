[package]
name = "server_pir"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
tokio-postgres = "0.7"
shared = { path = "../shared" }
rand = "0.9"
rand_chacha = "0.9" 
ndarray = "0.16"
serde = { version = "1.0", features = ["derive"] }
num-integer = "0.1"
once_cell = "1.18"
